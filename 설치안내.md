# 가계부 앱 설치 안내

컴퓨터·폰 어디서나 두 사람이 같은 데이터를 공유하는 가계부입니다.
순서: **① 구글 시트 저장소 만들기 → ② 앱에 주소 연결 → ③ 웹에 올리기 → ④ 폰에 앱으로 추가**

받은 파일 (7개 + 안내 2개):
`index.html` · `sw.js` · `manifest.webmanifest` · `apple-touch-icon.png` · `icon-192.png` · `icon-512.png` · `icon-maskable-512.png` · `apps-script.gs` · `설치안내.md`

---

## ① 구글 시트 저장소 만들기 (약 5분)

1. 구글 드라이브에서 **새 구글 스프레드시트**를 하나 만듭니다. (이름 예: `가계부 저장소`)
2. 상단 메뉴 **확장 프로그램 → Apps Script** 클릭.
3. 열린 편집기의 기본 코드를 모두 지우고, `apps-script.gs` 내용을 **전부 붙여넣기** 후 저장(디스크 아이콘).
4. 우측 상단 **배포 → 새 배포** 클릭.
   - 톱니바퀴(유형 선택) → **웹 앱** 선택
   - **실행 계정: 나**
   - **액세스 권한: 모든 사용자**
   - **배포** 클릭 → 권한 승인(내 계정 허용).
5. 배포 완료 화면의 **웹 앱 URL**을 복사합니다. (`.../exec` 로 끝나는 긴 주소)

> 이 URL이 앱과 시트를 잇는 통로입니다. 잘 보관하세요.

---

## ② 앱에 주소 연결

1. `index.html` 을 텍스트 편집기로 엽니다. (메모장, VS Code 등)
2. 위쪽에서 이 줄을 찾습니다:

   ```
   const API = "";
   ```

3. 따옴표 안에 ①에서 복사한 URL을 붙여넣습니다:

   ```
   const API = "https://script.google.com/macros/s/....../exec";
   ```

4. 저장.

> `TOKEN = "milliner-budget"` 은 그대로 두면 됩니다.
> 바꾸고 싶으면 `index.html` 과 `apps-script.gs` **양쪽** 값을 똑같이 바꾸세요.

---

## ③ 웹에 올리기 (둘 중 하나)

두 사람이 **같은 주소**를 열어야 데이터가 공유됩니다.
7개 파일(`index.html`, `sw.js`, `manifest.webmanifest`, 아이콘 4개)을 **한 폴더 최상단**에 함께 올립니다.

### 방법 A — Netlify (제일 쉬움, 계정 없이 가능)

1. 7개 파일을 한 폴더에 모아 zip 압축.
2. `app.netlify.com/drop` 접속.
3. zip을 끌어다 놓기 → 바로 `https://이름.netlify.app` 주소 발급.

### 방법 B — GitHub Pages

1. github.com 에서 **Public** 저장소 생성.
2. "uploading an existing file" 로 7개 파일 드래그 업로드 → Commit.
3. **Settings → Pages → Source: Deploy from a branch → main / (root)** 저장.
4. 1~2분 뒤 `https://아이디.github.io/저장소이름/` 생성.

> 주의: 파일은 하위 폴더가 아니라 **최상단**에. `sw.js`·`manifest` 가 상대경로라서요.
> `index.html` 을 수정해 다시 올리면, 앱을 껐다 켜면 자동으로 최신으로 갱신됩니다.

---

## ④ 폰에 앱으로 추가

두 사람 각자 자기 폰에서 ③의 주소를 열고:

- **아이폰(Safari)**: 공유 버튼 → "홈 화면에 추가"
- **안드로이드(Chrome)**: ⋮ → "앱 설치" 또는 "홈 화면에 추가"

홈 화면 아이콘으로 열면 주소창 없이 전체화면 앱처럼 뜹니다.
컴퓨터에서도 같은 주소를 열면 됩니다.

---

## 어떻게 공유되나 (알아둘 점)

- 기록/수정은 구글 시트에 저장되고, **20초마다·화면을 켤 때마다** 서로 반영됩니다. 즉시 실시간은 아니지만 곧 맞춰집니다.
- 두 사람이 **서로 다른 지출**을 추가하면 둘 다 남습니다(합쳐짐). 삭제도 반영됩니다.
- 단, **똑같은 항목**을 동시에 고치거나 **예산 값**을 동시에 바꾸면 마지막에 저장된 쪽이 이깁니다. 예산 수정은 한 사람이 맡는 걸 권합니다.
- 인터넷이 없으면 그 기기에 임시 저장되고, 다시 연결되면 동기화됩니다.
- 저장 용량: 시트 한 칸 한도(약 5만 자)까지. 수년치 지출도 충분하지만, 언젠가 넘치면 저장 방식을 파일로 바꾸면 됩니다.

## 개인정보

- 배포 권한을 "모든 사용자"로 두면 URL을 아는 사람은 접근할 수 있습니다. 주소는 추측 불가능하지만 공개 링크입니다.
- 가계부라 민감도가 낮지만, 주소와 TOKEN은 두 사람만 공유하세요.
